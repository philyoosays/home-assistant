module.exports = {
  myName(result) {
    if(result.toLowerCase() === 'helios') {
      return true
    } else {
      return false
    }
  },
  speechRouter(req, res, next) {
    let thePackage = req.body
    switch(true) {
      default:
        break;
    }
  },
}
